d = 2;
c = 3;
n = 500;

[X,label] = gausseanMixtureGenerator(d,c,n);
figure(1);
plotClass(X,label);
hold on
centers = kmeans(X,c);
plot(centers,

[z, model] = mixGaussEm(X,centers);
figure(3);
plotClass(X,z);